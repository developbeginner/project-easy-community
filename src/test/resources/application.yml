# application.yml
# this is test profile used in test env

# DB datasource settings
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test_db
    username: sa
    password: